AI Data Agent

A beginner-friendly Python agent that uses LangChain and OpenAI to analyze data from CSV files automatically.

How to Run
1. Clone this repo
2. Create a virtual environment
3. Install dependencies:
   ```bash
   pip install -r requirements.txt
